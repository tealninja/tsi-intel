{
  "name": "tsi-intel-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server for the TSI-Intel app — read/write the sales pipeline (opportunities) on the tsi-intel-api Worker, and search the bundled accounts/contacts/products reference data.",
  "type": "module",
  "bin": {
    "tsi-intel-mcp": "src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node test/smoke.mjs && node test/pipeline-worker.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
